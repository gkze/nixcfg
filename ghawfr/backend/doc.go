// Package backend defines generic execution backend interfaces, worker/provider
// planning, and concrete backend implementations.
//
// Higher-level action-step adapters live outside this package so backend stays
// focused on worker semantics rather than curated uses: behavior.
package backend
