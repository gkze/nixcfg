______________________________________________________________________

## description: Update all flake inputs that use git tags to their latest versions

For every input in @flake.nix that has a git tag, make sure that we're using the latest tag.
